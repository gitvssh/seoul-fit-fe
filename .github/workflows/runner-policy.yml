name: Runner policy

on:
  push:
    branches: [master]
    paths:
      - ".github/workflows/**"
      - ".github/scripts/**"
  pull_request:
    branches: [master]
    paths:
      - ".github/workflows/**"
      - ".github/scripts/**"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: seoul-fit-frontend-runner-policy-${{ github.ref }}
  cancel-in-progress: true

jobs:
  policy:
    name: Self-hosted runner policy
    runs-on: homelab-seoul-fit-fe
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with: {persist-credentials: false, fetch-depth: 1}
      - name: Reject GitHub-hosted or dynamic runner labels
        run: python3 .github/scripts/validate_workflow_runners.py homelab-seoul-fit-fe
      - name: Test the policy validator
        run: python3 -m unittest discover -s .github/scripts/tests -p "test_*.py"
